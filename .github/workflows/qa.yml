name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run Unit Tests
      run: make test
    - name: Build Production bundle
      run: make prod
